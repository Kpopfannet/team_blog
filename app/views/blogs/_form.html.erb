<%= form_for(blog) do |f| %>
    <% if blog.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>
            <% blog.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
        </div>
    <% end %>

    <div class="row k_padding h">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-7">
                    <%= f.text_area :content, class: "tinymce_area"%>
                </div>
                <div class="col-md-1"></div>
                <div class="k_padding h v col-md-4">
                    사진등록
                </div>
            </div>
        </div>
    </div>   

    <%= f.button("", {class: "k_button f c"}) do%>
        <div class="body r kakao_color">
            <p class="text">밥 등록</p>
        </div>
    <%end%>

<% end %>

